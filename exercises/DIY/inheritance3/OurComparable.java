public interface OurComparable {
    /**
     *
     * negative if this < o
     * 0 if this = o
     * positive if this > o
     *
     * = int compareTo(OurComparable o);
     */
    int compareTo(Object o);
}
